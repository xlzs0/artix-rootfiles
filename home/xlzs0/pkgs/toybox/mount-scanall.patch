--- a/lib/commas.c	2025-10-14 19:45:35.000000000 +0300
+++ b/lib/commas.c	2026-05-19 02:53:02.365985877 +0300
@@ -89,8 +89,10 @@
 
   while (scanlist && *scanlist) {
     char *opt = comma_iterate(&scanlist, &i), *s = xstrndup(opt, i);
+    int no = !strncmp(s, "no", 2);
 
-    i = comma_scan(optlist, s, 0);
+    i = comma_scan(optlist, s+no*2, 0);
+    if (no) i = !i;
     free(s);
     if (!i) break;
   }
